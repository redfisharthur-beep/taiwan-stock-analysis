# Names only: NEVER commit real values. Render > service > Environment.
SJ_API_KEY=REPLACE_ONLY_IN_RENDER
SJ_SEC_KEY=REPLACE_ONLY_IN_RENDER
# Independent random secret >= 32 chars shared with Cloudflare; NOT your brokerage secret.
SJ_BRIDGE_TOKEN=SET_A_LONG_RANDOM_STRING_IN_RENDER_AND_CLOUDFLARE
